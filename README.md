# Microservice Jira to Planfix

## 🚀 Какую задачу решает данный проект?
Данный проект создан для решение такой задачи: "Нам нужно связать наш аккаунт в Jira с Planfix. Все поставленные задачи на
пользователя в Jira должны отображаться в Planfix, так же, все комментарии и вложенные файлы."
Найденные ресурсы для связи Jira + Planfix в большинстве случаев предлагают создавать задачу в Planfix с ссылкой на задачу в Jira, что в свою очередь не подходит нам(слишком уж муторно переходить по ссылкам из одной системы в другую).

### Что мы имеем? Как это работает?
Реализация задачки по своему очень проста. Нам дали ограниченный вариант некоторых ресурсов со стороны Jira(бюрократия), только токен с ограниченными правами на  чтение для работы с задачами и <a href="https://developer.atlassian.com/cloud/jira/platform/rest/v3/intro/#about">открытая документация</a>.
С Planfix все обстоит лучше, нам дали полный доступ к системе и у нас присутствует <a href="https://planfix.com/ru/help/API_ПланФикса">открытая документация</a>.

Основные переменные которые мы должны иметь со стороны Jira это ([ссылка на получение токена](https://id.atlassian.com/manage-profile/security/api-tokens)):
```text
JIRA_TOKEN=
JIRA_USERNAME=
```
TOKEN должен иметь права как минимум read:jira-work.  
Поле JIRA_USERNAME должно выглядеть так: username@tripster.ru

Основные переменные которые мы должны иметь со стороны Planfix это:
```text
PLANFIX_LOGIN= #Логин пользователя от имени которого будет поставлена задача
PLANFIX_PASSWORD= #Пароль данного пользователя
PLANFIX_API_KEY= 
PLANFIX_ACCOUNT= #Аккаунт в Planfix
PLANFIX_MEMBERS= #ID участников задачи(прим. 12345,214123,41231)
PLANFIX_WORKERS= #ID исполнителей(прим. 241234,124212,55321)
PLANFIX_OWNER_COMMENT= #ID пользователя, от имени которого будет оставлен комментарий
PLANFIX_PROJECT_ID= #ID проекта, где будут задачи
```
Для полученья токена для XML API, нужно зайти: «управление аккаунтом → доступ к api → XML AP»(доступно только на платных тарифах). И там получить API key.  
Для полученья ID всех пользователей можно обратиться к [документации](https://planfix.com/ru/help/ПланФикс_API_user.getList).   
Для полученья PLANFIX_PROJECT_ID уже из ссылки созданного проекта https://username.planfix.ru/?action=tasks&project=81484 можно взять 81484.

Работает проект так: 
- В цикле по-установленному SLEEP_INTERVAL мы обращаемся к Jira API и получаем список всех задач(кроме задач в статусе "On pause / Blocked") где назначен наш пользователь.
- Хэшируем значение полей.
- Получаем временный SID Planfix(TTL=1 час).
- Начинаем работать с Redis. Redis отвечает за хранения ключ: хэш значений которые могут измениться(описание задачи, вложения, комментарии). 
Мы подгружаем все значения по ключу из ранее полученных задач Jira.
- Далее сверяем ID задач. Создаем список задач которые уже отслеживаем, новые задачи, измененные.
- Если задачи присутствуют в Redis, мы сравниваем хэш description, attachment текущей задачи с данными из Redis. Если хэш равный, то пропускаем задачу.
Если хэш разный, то добавляем в список измененных и данные об изменениях(id: int, h_description: bool,
h_attachment: bool). И перезаписываем измененный хэш в Redis.
- Из списка измененных задач мы знаем какие поля изменились, и отправляем свежие данные в Planfix к уже созданной ранее задаче.
- Если у нас появились новые задачи, то мы создаем новую задачу в Planfix, отправляя все нужные данные(title, description, 
attachment). В ответ получаем ID Planfix задачи и записываем связку Jira issue ID + Planfix task ID в Redis, так же хэшируем нужные нам поля.
- Работа с комментариями. Если есть комментарии в задачах которые отслеживаются, загружаем комментарии из Jira.
Если комментарий отсутствует в Redis, то он отсутствует и в Planfix, по API Planfix добавляем его к указанной задаче, и 
записываем в Redis: comment_jira_id, comment_planfix_id, planfix_issue_id, h_description. Если комментарий присутствует 
в Redis, мы сравниваем хэш значения description текущего комментария с h_description из Redis. Если хэш разный, то 
обновляем комментарий в Planfix и обновляем хэш значение в Redis. Если хэш равен, то пропускаем.
- В каждых критичных точках у нас стоит оповещение с помощью Телеграмм бота, в наш чат с ошибками.  

Данный проект решает конкретно поставленную задачу, мы получаем все задачи назначенные на указанного пользователя в Jira и дублируем их в Planfix, начиная от названия до комментариев.
В этом проекте отсутствует гибкость, в таких местах, как функционал индивидуального назначения разных исполнителей, участников, разделение задач на разные проекты и многое другое. И возможно проект будет улучшатся со временем. 

## ⚙️ Установка
Требования:
 - docker 24+ и docker compose v2+
 - Ubuntu 22+

 ### 1. Подготовка проекта
```bash
    sudo mkdir -p /home/service/msvc-jira-to-planfix
    sudo chown $USER:$USER /home/service/msvc-jira-to-planfix
    cd /home/service/msvc-jira-to-planfix
```

### 2. Заполняем .env файл чувствительными данными
```text
JIRA_TOKEN=
JIRA_USERNAME=
JIRA_URL_SEARCH_ISSUES=https://user.atlassian.net/rest/api/3/search/jql
JIRA_URL_ATTACHMENT_ISSUES=https://user.atlassian.net/rest/api/3/attachment/content/{id}
JIRA_URL_ISSUE_LINK=https://user.atlassian.net/browse/{key}
JIRA_URL_GET_COMMENTS=https://user.atlassian.net/rest/api/3/issue/{id}/comment

PLANFIX_API_KEY=
PLANFIX_URL=https://apiru.planfix.ru/xml/
PLANFIX_ACCOUNT=
PLANFIX_LOGIN=
PLANFIX_PASSWORD=
PLANFIX_MEMBERS=11234,325741,145761
PLANFIX_WORKERS=11234,325741,145761
PLANFIX_OWNER_COMMENT=123245
PLANFIX_PROJECT_ID=00000

BOT_TOKEN=
BOT_CHAT_ID=

SLEEP_INTERVAL=14400

REDIS_HOST=redis
REDIS_PORT=6379
```

### 3. Настройка Docker

Проверяем docker-compose.yaml.
```text
services:

  msvc-jira-planfix:
    image: morarsv/msvc-jira-to-planfix:latest
    container_name: msvc-jira-planfix
    restart: unless-stopped
    env_file: ".env"
    volumes:
      - /home/service/msvc-jira-to-planfix/downloads:/app/downloads
      - /home/service/msvc-jira-to-planfix/logs:/app/logs
    depends_on:
      - redis
    networks:
      - msvc-net


  redis:
    image: "redis:7"
    container_name: redis
    restart: unless-stopped
    healthcheck:
      test: [ "CMD", "redis-cli","ping" ]
    networks:
      - msvc-net


networks:
  msvc-net:
```

### 4. Запуск всех сервисов

```bash
    sudo docker compose up -d
    docker compose ps
```